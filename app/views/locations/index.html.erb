<div>
  <h1 class="font-bold text-4xl">Locations#index</h1>
  <p>Find me in app/views/locations/index.html.erb</p>
</div>

<%= paginate @locations %>

<div>
  <ul>
    <% @locations.each do |location| %>
      <li>
        <%= link_to location['name'], location_path(location['name']) %>
      </li>
      <li>
        <%= location.zoneCount %>
      </li>
      
    <% end %>
  </ul>
</div>

<%= link_to 'Back to Monsters List', root_path %>