<div>
  <h1 class="font-bold text-4xl">Ailments#index</h1>
  <p>Find me in app/views/ailments/index.html.erb</p>
</div>

<%= paginate @ailments %>

<div>
  <ul>
    <% @ailments.each do |ailment| %>
      <li>
        <%= link_to ailment['name'], ailment_path(ailment['name']) %>
      </li>
    <% end %>
  </ul>
</div>

<%= link_to 'Back to Monsters List', root_path %>